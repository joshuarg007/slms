# Site2CRM Frontend Environment Variables
# Copy this file to .env.local for development or .env.production for production

# API URL
# Development: Use "/api" to leverage Vite's proxy (configured in vite.config.ts)
# Production: Full URL to your API server (e.g., https://api.site2crm.io/api)
VITE_API_URL=/api

# Stripe Publishable Key
# Get this from your Stripe Dashboard -> Developers -> API Keys
# Use test key (pk_test_...) for development, live key (pk_live_...) for production
# IMPORTANT: Never commit live keys to git - inject at deploy time
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here
